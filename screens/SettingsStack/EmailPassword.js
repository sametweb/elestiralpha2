import React from "react";
import { View, Text, ScrollView } from "react-native";

const EmailPassword = props => {
  return (
    <View>
      <Text>EmailPassword</Text>
    </View>
  );
};

export default EmailPassword;
